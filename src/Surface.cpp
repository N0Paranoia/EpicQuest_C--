#include "Surface.h"

Surface::Surface()
{
    SDL_Surface* Surface::Onload(char* File)
    {
        SDL_Surface* Surf_Temp = nullptr;
        SDL_Surface* Surf_Return = nullptr;

        if((Surf_Temp = SDL))
    }
}
